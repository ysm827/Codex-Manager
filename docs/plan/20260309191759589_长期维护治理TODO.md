# 长期维护治理 TODO

## 使用说明

本清单按优先级排序，目标不是一次性做完，而是作为未来 4~8 周的治理待办。

状态建议：

- `TODO`：未开始
- `DOING`：进行中
- `DONE`：已完成
- `WONTFIX`：确认不做

## P0 治理基建

- [x] 恢复 `docs/` 正式版本管理，只忽略临时报告，不再忽略整个 `docs/` 目录。
- [x] 新增 `docs/README.md`，说明 `plan / decision / release / report` 四类文档的提交规则。
- [x] 新增仓库级 `TESTING.md`，定义前端、Rust、协议适配、workflow 改动的最小验证矩阵。
- [x] 新增仓库级 `SECURITY.md`，写清安全问题反馈入口和敏感数据处理约束。
- [x] 新增 `.github/ISSUE_TEMPLATE/bug_report.yml`。
- [x] 新增 `.github/ISSUE_TEMPLATE/feature_request.yml`。
- [x] 新增 `.github/pull_request_template.md`。

## P1 入口文件瘦身

- [x] 拆分 `apps/src/main.js`，让它只保留启动装配与模块组合。
- [x] 拆分 `apps/src/settings/controller.js`，按设置域拆为多个子模块。
- [x] 拆分 `apps/src-tauri/src/lib.rs`，把 Tauri commands 移入 `commands/` 目录。
- [x] 拆分 `crates/service/src/lib.rs`，把设置 key、bind mode、bootstrap、runtime 配置进一步抽离。
- [x] 评估 `crates/service/src/gateway/observability/http_bridge.rs` 的再拆分方案。
- [x] 评估 `crates/service/src/gateway/protocol_adapter/request_mapping.rs` 的按协议/输入类型拆分方案。
- [x] 评估 `crates/service/src/gateway/protocol_adapter/response_conversion/sse_conversion.rs` 的按适配目标拆分方案。

## P1 文档职责收敛

- [x] 缩短 `README.md`，只保留项目介绍、快速开始、产物入口、文档索引。
- [x] 同步缩短 `README.en.md`，保持与中文 README 的职责一致。
- [x] 把发布体系补充文档下沉到 `docs/release/`，减少 README 持续膨胀。
- [x] 给 `ARCHITECTURE.md` 增加“核心复杂域入口索引”，特别是 `gateway/` 链路。

## P2 测试治理

- [x] 新增 `apps/TESTING.md`，说明 `src/**/tests` 与 `apps/tests` 的边界。
- [x] 新增 `crates/service/tests/README.md`，区分单元测试、HTTP/RPC 集成测试、协议兼容回归测试。
- [x] 给 `scripts/tests/` 增加说明文档，标记哪些探针是“本地手工验证”、哪些可进入 CI。
- [x] 为协议兼容路径整理一份固定回归清单：`/v1/responses`、`/v1/chat/completions`、stream、non-stream、tools/tool_calls。

## P2 脚本与发布治理

- [x] 新增 `scripts/README.md`，按“开发 / 测试 / 发布 / 仅 CI”分类整理脚本入口。
- [x] 给 `.github/actions/` 与 `scripts/release/` 增加职责对照表，明确 workflow、action、script 的边界。
- [x] 为 `release-all.yml` 增加一份旁路说明文档，记录输入、产物、失败排查路径。
- [x] 清点 `scripts/` 下是否仍有历史兼容脚本只剩单点用途，能否进一步归档或重命名。

## P3 正式项目观感

- [x] 为 `crates/service/src/gateway/` 补一份目录级 README，描述请求流转与模块关系。
- [x] 为 `apps/src/` 增加模块索引文档，说明 `services / settings / ui / views` 的边界。
- [x] 统一测试文件命名与文案，消除 `.test.js`、`.test.mjs` 并存带来的认知负担。
- [x] 对超大文件建立预警阈值并写入 `CONTRIBUTING.md`，形成长期约束。

## 建议里程碑

## 里程碑 A：治理基础成型

- 恢复 `docs/` 版本管理
- 补 `TESTING.md`
- 补 `SECURITY.md`
- 补 issue / PR 模板

## 里程碑 B：入口文件拆分

- 拆 `apps/src/main.js`
- 拆 `apps/src/settings/controller.js`
- 拆 `apps/src-tauri/src/lib.rs`
- 拆 `crates/service/src/lib.rs`

## 里程碑 C：复杂域可维护化

- 给 `gateway/` 补目录说明
- 给测试体系补分层说明
- 给发布链路补架构说明

## 验收标准

- 新协作者能从仓库文档直接找到：
  - 项目入口
  - 构建方式
  - 测试方式
  - 发布方式
  - 核心复杂模块说明
- 新增功能不再默认追加到 `main.js`、`lib.rs`、workflow 大文件。
- 治理文档能稳定提交到仓库，而不是只停留在本地。









